{
	"name": "hub",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"generate:ai-txt": "node ../../packages/shared/scripts/generate-ai-txt.mjs",
		"dev": "pnpm generate:ai-txt && next dev",
		"build": "pnpm generate:ai-txt && pnpm --filter @ak-apps/shared generate:assets && next build",
		"start": "next start",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@ak-apps/shared": "workspace:*"
	}
}
